what I did today is to have the two distances (mahala and euclidean) calculated in a pretty file tha does: 
1. calculates the median 
2. calculates the distance of every embedding to the median
3. makes confusion matrix of them 
4. filters out the data and saves it to a npz file 
5. calculates the new conf matrix with the original medians, to make sure. 
    had to be the original ones as calculating new ones would change the results (as the data is less than before)

I plotted tsne visualisations of normalised and not nromalised, resnet and dino for both types, and they are practically the same for embedding extractors. 

compared to the original non-filtered data, the tsne vis is much cleaner. less overlap of data, so yay!

then I tested to see if doing k means on the tsne decreased dimensions brought any improvement, and it did not. 
I also tested the k-medoids, and it did not improve the clustering. 

I checked other clustering algorithms, and didnt find much, but I need to check more. 
the v-measure that stefan suggested is similar to normalised mutual information, which is less good than ami, as it does not account for chance(check)

so now I have to figure out what to do, as the best result is the one with filtered data with k means. I could run the other clustering algs and see what the results are 

agglomerative_clustering with the euclidean distance did a great job with dino and got 70% ami score. which is the same as the original baseline! 

we will look at the hdbscan, but, mahalanobis one did not do as well, so I might not run it until we discuss. 

