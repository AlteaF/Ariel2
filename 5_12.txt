we talked about many things, some of the results from the calculations, the way that the tsne vis looks, and how the different small clusters could be background differences, or temporal information

especially temporal information might be super interesting, as there are only tiny clusters for the fish that are stationary, such as starfish, crab, and black goby, which lays on the bottom of the sea bed and chills a lot. 



the question then became, why does the performance increase for the clustering, when we filter the data? what kind of data is filtered, that becomes so noisy? 

so, what we decided to do is: 
do a series of histograms on the data, specifically:
- on the full data: overview of area and aspect ratio. 
- for each class: area and aspect ratio 
        these are to see what kind of fish shapes thereare (if a long fish, the aspect ratio will be rectangular in a certain way. )

thenfor the filtered data do histograms and for the filtered out ones too. and compare the histograms. maybe do an overlap of the two to see if tehre is any 



overall the problem is, how can we take away the labelling burden? what if we found a way to have very cose together clusters that are very similar, sowe can label a few instances and have the whole thing labelled? 

how certain can be be of the cluster groupings? think about the concentric drawing. maybe you can only trust the first 40% of the data clustered around the centre, and not the rest. 


so next time I appraoch this: 
get the filtered out data and the filtered in data from the two files
make a file that, given a txt file with paths to a set of images, gets the area and the aspect ratio and plots an histogram of them. 
this file can then be used for all the different combinations :) 


also, run the umap and the k medoids on full data 


DONE! 